PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173
JWT_SECRET=smartride_super_secret_jwt_key_phase1_2026!
JWT_EXPIRES_IN=7d
# Optional: Provide MongoDB Atlas URI, otherwise local memory MongoDB is used in dev
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/smartride?retryWrites=true&w=majority
MONGODB_URI=
